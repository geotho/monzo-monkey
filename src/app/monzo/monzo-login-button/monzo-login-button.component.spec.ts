/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MonzoLoginButtonComponent } from './monzo-login-button.component';

describe('Component: MonzoLoginButton', () => {
  it('should create an instance', () => {
    let component = new MonzoLoginButtonComponent();
    expect(component).toBeTruthy();
  });
});
